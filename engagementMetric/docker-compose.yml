version: '3'
services:
  backend: 
  
    image: rnolas96/prod-engagementmetric-backend:prod  
    container_name: api-container-engagement
    environment:
      - TAIGA_URL=https://api.taiga.io/api/v1
    ports:
      - '8005:8000'
  
  frontend:
    image: rnolas96/prod-engagementmetric-frontend:prod
    container_name: gui-container-engagement
    ports:
      - '3005:3000'
    links:
      - backend 
    stdin_open: true
    tty: true
volumes: 
  dev-redis_data: